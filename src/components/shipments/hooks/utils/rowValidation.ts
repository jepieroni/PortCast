// This file is now obsolete with the new bulk upload system
// Keeping as placeholder for any remaining references

export const validateRow = (row: any) => {
  return { isValid: false, errors: ['Using legacy validation - please use new system'] };
};

export const translateRow = (row: any) => {
  return { success: false, errors: ['Using legacy translation - please use new system'] };
};
